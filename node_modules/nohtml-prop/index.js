class Prop {
    static use (el, v) {
        for (let i in v) {
            let prop = short[i];
            switch (typeof prop) {
                case 'string':
                    el[prop] = v[i];
                    break;
                case 'function':
                    prop(el, v[i]);
                    break;
                default:
                    el[i] = v[i];
            }
        }
    }
};
var short = {
    t: 'textContent',
    h: 'innerHTML',
    v: 'value',
    p: 'placeholder',
    c: 'className',
    a: (el, v) => {
        for (let i in v) {
            el.setAttribute(i, v[i]);
        }
    },
    s: (el, v) => {
        for (let i in v) {
            el.style[i] = v[i];
        }
    },
    i: (el, v) => {
        v.appendChild(el);
    }
};
module.exports = Prop;